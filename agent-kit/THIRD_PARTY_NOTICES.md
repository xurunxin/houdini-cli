# 第三方资料说明

本包是原创结构化摘要、检索索引、操作建议和新编写代码，不是 SideFX、CGWiki 或其他作者发布的官方资料包。

Houdini、Houdini Engine、Karma、SideFX Labs 等名称属于相关权利人。官方文档、Tokeru/CGWiki、John Kunz、Entagma 的原页面/图片/视频/场景依各自许可使用。本包只给可追溯链接和原创归纳，不包含第三方HIP、付费视频、截图或整站副本。

`houdini-cli` 与 `houdini-mcp` 的源码用于核对接口和行为；本包不重新分发其源码或替用户授权访问私有仓库。固定提交和文件位置在sources.lock.json。用户仍需要自己的仓库访问权、Houdini安装与适用许可证。

可选的 `jsonschema`（仅已安装时用于更完整参数校验）不随包分发。未安装时使用有限基础schema检查并明确报告限制。所有其他本包离线Python工具使用标准库。

加载未知HIP/HDA/脚本可能运行回调和外部操作。公开可下载不等于安全可执行；遵循审查、许可和场景保护规则。
