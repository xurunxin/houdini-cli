---
name: houdini-pdg
description: Houdini TOP/PDG变体、作业依赖、缓存和交付；不并行抢占同一个GUI桥接。
---

# houdini-pdg

## 按需资料

依赖同目录安装的 `houdini-agent` 知识目录；直接使用本技能，无需先加载总路由。
只读与问题相关的卡片；已有准确入口时无需再检索。查询或写方案不启动 Houdini。

- [按需阅读：PDG / TOPs：批量任务、变体与依赖](../houdini-agent/wiki/26-pdg.md)
- [按需阅读：缓存、导入导出、路径与交付](../houdini-agent/wiki/27-cache-io.md)

## 数据与取舍

- 输入契约：输入清单、work item属性、依赖、输出文件命名、scheduler、许可证/内存/设备并发预算。
- 先独立完成一个work item，然后扩展wedge。每个变体的输出包含任务/资产/版本/seed，防止路径冲突。
- 仅可重复且状态已知的任务允许调度重试。CLI超时未知的GUI写操作不属于安全自动重试对象。

## 现场执行

实际操作按 [CLI 契约](../houdini-agent/wiki/02-cli-contract.md) 执行；环境未变时复用已核对的 schema 与节点信息。保留无关现场，在授权命名空间内串行修改、读回并修复到结果通过；超时未知操作先查状态，不重放。

## 执行任务完成条件

- 每个item的输入、属性、输出和日志可追踪。
- 一个item失败不会覆盖其他变体的输出。
- 小批次通过后才提高并发，许可证与内存有现场验证。

## 定位失败

- 输出互相覆盖：路径缺少wedge/item/版本字段。
- 看起来成功但拿到旧缓存：核对输入指纹和cache命中依据。

## 返回结果

交付所请求的结论、代码或产物；现场操作附改动、路径与证据。标明技术、时间、视觉未验证项，离线检查不代表 Houdini 运行通过；受阻时保留产物和具体阻塞。
