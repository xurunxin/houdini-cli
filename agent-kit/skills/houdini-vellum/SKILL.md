---
name: houdini-vellum
description: 布、绳、软约束与Vellum穿透/拉伸诊断；烟水不触发此skill。 输出最小可验证方案、必要网络变更和带证据的验收结果。
---

# houdini-vellum

## 激活与依赖
布、绳、软约束与Vellum穿透/拉伸诊断；烟水不触发此skill。
必须同目录安装 `houdini-agent`。先遵循 [执行协议](../houdini-agent/wiki/02-cli-contract.md)，不替代原有 `houdini-cli` 的环境和生命周期能力。

## 开始前
确认目标、已有节点路径、Houdini版本、输入数据、输出格式及允许修改范围。只有阻塞安全执行的信息才询问；其余列明假设并从低成本原型开始。

## 按需知识
- [按需阅读：Vellum：布料、绳索、软体与颗粒](../houdini-agent/wiki/12-vellum.md)

## 工作顺序
1. 用 `houdini-agent/tools/query.py search` 检索当前问题，只读取命中卡片；读取现场工具schema和节点参数。
2. 检查领域输入：模拟几何、约束流、碰撞流；边长分布、厚度、质量、pin与目标动画、预期材料。
3. 写清数据流和修改计划。为布面、曲线/绳、体积软体选择对应的约束构造方式。geometry和constraints为不同数据流；核对当前solver的输入标签。
4. 在用户授权范围或任务命名空间做最小试验，串行调用；不重置全场景，不覆盖无关节点。
5. 每阶段检查真实数据再继续。先修碰撞体质量和速度采样，再调子步、迭代、刚度；每次改变一组参数，保存对照。最终平滑/细分与模拟分辨率分离。
6. 提交证据，记录失败/未验证项，结束会话时默认保留应用。

## 必须验收
- 几何与约束相匹配，pin位置与意图一致。
- 连续帧检查穿透、拉伸、抖动；记录检查帧段与容忍标准。
- 有可回放缓存及低/高质量设置差异。

## 失败与降级
- 高速碰撞穿透：先查碰撞厚度/初始重叠/变形采样，再增加子步；不要只提高constraint iterations。
- 布无限拉长：检查约束是否存在、pin目标、刚度和收敛。
- 绳子像薄布塌下：确认使用曲线/拉伸弯曲约束，而非照搬cloth设置。
工具缺失时先确认 schema；专用能力不存在才考虑经检查的 HOM。`guide` 或 `discovery` 文档均不代表现场已经通过运行验收。超时先查状态，不重复提交未知写操作。

## 交付
输出：目标与假设、实际版本、网络/参数改动、缓存/渲染/导出路径、技术与视觉验收证据、未验证项及下一步检查。不得用本地Python语法测试冒充Houdini运行测试。
